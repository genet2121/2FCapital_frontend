
enum FieldTypes {
    TEXT = "text",
    EMAIL = "email",
    FILE = "file",
    SELECT = "select",
    REFERENCE = "reference",
    PASSWORD = "password",
    IMAGE = "image",
    TEXTAREA = "textarea",
    DATE = "date",
    DATETIME = "date-time",
    TIMESTAMP = "timestamp",
    NUMBER = "number"
}

export default FieldTypes;